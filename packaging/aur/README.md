# Arch/SteamOS (AUR)

Package type: `vm-remote-git` (builds from this repo).

Build (on Arch/SteamOS):
1) `cd packaging/aur`
2) `makepkg -si`

Noob-friendly shortcut:
- Run `./packaging/release_aur.sh` from repo root.

Dependencies:
- `python`
- `tk`
