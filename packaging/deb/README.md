# Debian/Ubuntu (.deb)

Build (on Debian/Ubuntu):
1) `cd packaging/deb`
2) `chmod +x build.sh`
3) `./build.sh`

Noob-friendly shortcut:
- Run `./packaging/release_deb.sh` from repo root.

Dependencies:
- `python3`
- `python3-tk`

Output:
- `packaging/deb/dist/vm-remote_<version>_all.deb`
